---
import { camps } from "../data/camps";
import { site } from "../data/site";
---

<section id="kempy" class="section camps" aria-labelledby="camps-title">
  <div class="container-x">
    <header class="section-header reveal">
      <span class="eyebrow">Pojďte s námi ven</span>
      <h2 id="camps-title">
        Kempy pro rodiče <span class="section-title-accent">s dětmi</span>
      </h2>
      <p class="section-subtitle">
        Společně strávený čas plný pohybu, tvoření a zábavy pro děti i rodiče.
      </p>
    </header>

    <ol class="camps__list">
      {
        camps.map((c, i) => (
          <li
            class={`camp camp--${c.accent} reveal`}
            style={`transition-delay: ${i * 100}ms`}
          >
            <div class="camp__body">
              <h3>{c.title}</h3>
              <p class="camp__place">
                <svg
                  viewBox="0 0 24 24"
                  width="16"
                  height="16"
                  aria-hidden="true"
                >
                  <path
                    d="M 12 22 C 12 22 4 14 4 9 C 4 4.5 7.5 1 12 1 C 16.5 1 20 4.5 20 9 C 20 14 12 22 12 22 Z M 12 12 C 13.6 12 15 10.6 15 9 C 15 7.4 13.6 6 12 6 C 10.4 6 9 7.4 9 9 C 9 10.6 10.4 12 12 12 Z"
                    fill="currentColor"
                  />
                </svg>
                {c.place}
              </p>
              <p class="camp__summary">{c.summary}</p>
            </div>

            <a
              href={site.reservationUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="camp__cta"
            >
              Více info
              <span aria-hidden="true">→</span>
            </a>
          </li>
        ))
      }
    </ol>
  </div>
</section>

<style>
  #kempy {
    scroll-margin-top: var(--section-scroll-margin);
  }
  .camps {
    background: linear-gradient(
      180deg,
      transparent 0%,
      var(--color-cream) 30%,
      var(--color-cream) 70%,
      transparent 100%
    );
  }

  .camps .section-title-accent {
    color: var(--color-lilac-deep);
  }

  .camps__list {
    list-style: none;
    padding: 0;
    margin: 0 auto;
    max-width: 60rem;
    display: grid;
    gap: var(--stack-gap);
  }

  .camp {
    --accent-color: var(--color-blush-vivid);

    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
    align-items: center;
    padding: var(--card-padding);
    background: white;
    border-radius: var(--radius-card);
    border: var(--card-border);
    transition:
      transform 220ms ease,
      border-color 220ms ease,
      box-shadow 220ms ease;
  }

  .camp--blush {
    --accent-color: var(--color-blush-vivid);
  }
  .camp--sky {
    --accent-color: var(--color-sky-deep);
  }
  .camp--lilac {
    --accent-color: var(--color-lilac-deep);
  }

  .camp:hover {
    transform: translateY(-2px);
    border-color: color-mix(in srgb, var(--accent-color) 40%, white);
    box-shadow: 0 18px 40px -18px
      color-mix(in srgb, var(--accent-color) 40%, transparent);
  }

  .camp__body h3 {
    font-size: var(--text-h3);
    margin: 0 0 0.4rem;
    line-height: var(--line-height-xl);
  }

  .camp__place {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--accent-color);
    font-size: var(--text-base);
    font-weight: 600;
    margin: 0 0 0.5rem;
  }

  .camp__summary {
    margin: 0;
    color: var(--color-ink);
    font-size: var(--text-base);
  }

  .camp__cta {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: var(--accent-color);
    font-weight: 700;
    text-decoration: none;
    border-bottom: 1.5px solid transparent;
    white-space: nowrap;
    transition: border-color 220ms ease;
    justify-self: start;
  }

  .camp__cta:hover {
    border-bottom-color: var(--accent-color);
  }

  @media (min-width: 1024px) {
    .camp {
      grid-template-columns: 1fr auto;
      gap: 1.5rem;
    }

    .camp__cta {
      justify-self: auto;
    }
  }
</style>
