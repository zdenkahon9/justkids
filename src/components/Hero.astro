---
import { site } from "../data/site";
---

<section id="hero" class="hero" aria-label="Úvod">
  <div class="hero__bg" aria-hidden="true">
    <div class="hero__photo"></div>
    <div class="hero__veil"></div>
  </div>

  <div class="container-x hero__inner">
    <div class="hero__copy">
      <h1 class="hero__title">
        Hravé cvičení<br />
        s láskou a klidem
      </h1>
      <p class="hero__lead">
        Pro miminka a děti od 3 měsíců do 3 let. Společně objevujeme svět
        pohybu, smyslů a radosti - bez stresu, ve vlastním tempu. Těšíme se na
        vás v
        <strong>Hořovicích</strong> a <strong>Zdicích</strong>.
      </p>

      <div class="hero__cta">
        <a
          href={site.reservationUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="btn btn-primary"
          data-testid="hero-cta"
        >
          Rezervovat lekci
          <span aria-hidden="true">→</span>
        </a>
        <a href="#vekove-skupiny" class="btn btn-ghost"> Prohlédnout kurzy </a>
      </div>
    </div>

    <div class="hero__media">
      <div class="hero__logo-wrap">
        <img
          src="/images/logo.png"
          alt="Logo JustKids"
          width="384"
          height="384"
          class="hero__logo"
          decoding="async"
        />
      </div>
    </div>
  </div>

  <a href="#o-mne" class="hero__scroll" aria-label="Pokračovat dolů">
    <span></span>
  </a>

  <div class="hero__wave" aria-hidden="true">
    <svg
      class="hero__wave-svg"
      viewBox="0 0 1440 100"
      preserveAspectRatio="xMidYMax meet"
      xmlns="http://www.w3.org/2000/svg"
    >
      <path
        fill="var(--color-cream)"
        d="M0,51 C180,51 340,68 540,68 C720,68 820,51 920,51 C1080,51 1240,81 1440,85 L1440,100 L0,100 Z"
      ></path>
    </svg>
  </div>
</section>

<style>
  .hero {
    /* Uniform scale (meet): height tracks viewBox aspect 100/1440 so steepness does not explode on narrow widths.
       Floor keeps iPhone SE from a paper-thin divider; no hard max — caps would force horizontal gutters with meet */
    --hero-wave-h: max(2.75rem, calc(100svw * 100 / 1440));
    position: relative;
    box-sizing: border-box;
    /* Full viewport height; fixed nav overlaps only the top — do NOT subtract nav from height (that caused the gap under the wave) */
    min-height: 100svh;
    height: auto;
    display: flex;
    align-items: center;
    padding: 0;
    padding-top: var(--nav-height);
    overflow: hidden;
    isolation: isolate;
    /* gap under lifted wave — same value on .hero::after + .hero__wave bottom */
    --hero-wave-mobile-lift: 0.75rem;
  }

  /* Cream strip under lifted wave — +2px / bottom:-1 kills subpixel hairline
     (rem/vh vs device px; z-index:-1 photo peeking between layers) */
  .hero::after {
    content: "";
    position: absolute;
    left: 0;
    right: 0;
    bottom: -1px;
    height: calc(var(--hero-wave-mobile-lift) + 2px);
    background: var(--color-cream);
    z-index: 0;
    pointer-events: none;
  }

  .hero__bg {
    position: absolute;
    inset: 0;
    z-index: -1;
    pointer-events: none;
  }

  .hero__photo {
    position: absolute;
    inset: -3%;
    background-image: url("/images/hero-balloons.png");
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    filter: blur(5px) saturate(1.05);
    transform: scale(1.06);
    will-change: transform;
    animation: hero-drift 22s ease-in-out infinite alternate;
  }

  @keyframes hero-drift {
    from {
      transform: scale(1.06) translate(0, 0);
    }
    to {
      transform: scale(1.08) translate(-1%, -1%);
    }
  }

  /* Soft pastel veil — keeps text readable, blends with brand colors.
     Slightly more saturated under the headline so the white lettering
     contrasts against richer pastels (no gray, no flat cream) */
  .hero__veil {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(
        48% 42% at 22% 32%,
        rgba(212, 191, 245, 0.55) 0%,
        rgba(212, 191, 245, 0) 70%
      ),
      radial-gradient(
        40% 40% at 30% 55%,
        rgba(252, 194, 214, 0.55) 0%,
        rgba(252, 194, 214, 0) 70%
      ),
      radial-gradient(
        55% 55% at 82% 30%,
        rgba(169, 212, 240, 0.4) 0%,
        rgba(169, 212, 240, 0) 70%
      ),
      linear-gradient(
        180deg,
        rgba(255, 249, 244, 0.2) 0%,
        rgba(255, 249, 244, 0.05) 35%,
        rgba(255, 249, 244, 0.45) 100%
      );
  }

  @keyframes float-slow {
    0%,
    100% {
      transform: translateY(0) rotate(0deg);
    }
    50% {
      transform: translateY(-22px) rotate(3deg);
    }
  }
  .hero__inner {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(2rem, 6vw, 5rem);
    align-items: center;
    width: 100%;
    text-align: center;
  }

  .hero__copy {
    animation: fade-up 800ms 100ms ease both;
  }

  @keyframes fade-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero__title {
    margin: 2rem 0 3rem;
    font-family: "Pacifico", "Inter", sans-serif;
    font-weight: 400;
    font-size: 3rem;
    line-height: 1.15;
    letter-spacing: 0;
    color: #f5f5f5;
    text-shadow:
      0 2px 18px rgba(42, 31, 56, 0.2),
      0 1px 3px rgba(42, 31, 56, 0.28);
  }

  .hero__lead {
    font-size: clamp(1.05rem, 1.4vw, 1.2rem);
    color: color-mix(in srgb, var(--color-ink-soft) 42%, var(--color-ink) 58%);
    max-width: 38rem;
    margin: 0 auto 3rem;
    padding-left: 1rem;
    text-align: left;
  }

  .hero__cta {
    display: flex;
    flex-wrap: wrap;
    gap: 1.2rem;
    margin-bottom: 3rem;
    justify-content: center;
  }

  .hero__pills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
    justify-content: center;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  /* Media side */
  .hero__media {
    display: none;
    place-items: center;
    animation: fade-up 800ms 250ms ease both;
  }

  .hero__logo-wrap {
    position: relative;
    width: min(100%, 384px);
    aspect-ratio: 1;
  }

  .hero__logo {
    width: 100%;
    height: 100%;
    object-fit: contain;
    filter: drop-shadow(0 30px 60px rgba(236, 47, 142, 0.35));
    animation: float-slow 8s ease-in-out infinite;
  }

  /* +1px: masks sub-pixel seam vs hero clip / compositor rounding */
  .hero__wave {
    position: absolute;
    left: 0;
    right: 0;
    bottom: var(--hero-wave-mobile-lift);
    z-index: 1;
    line-height: 0;
    pointer-events: none;
    transform: translateY(1px);
  }

  .hero__wave-svg {
    display: block;
    width: 100%;
    height: var(--hero-wave-h);
  }

  .hero__scroll {
    position: absolute;
    bottom: calc(var(--hero-wave-h) + 1rem);
    left: 50%;
    z-index: 3;
    transform: translate(-50%, 52px);
    width: 28px;
    height: 44px;
    border: 2px solid rgba(42, 31, 56, 0.25);
    border-radius: 14px;
    display: none;
    place-items: start center;
    padding-top: 8px;
    transition: border-color 220ms ease;
  }

  @media (min-width: 640px) and (max-width: 1023px) {
    .hero__title {
      font-size: 4rem;
      margin: 2rem 0 3.5rem;
    }

    .hero__lead {
      font-size: 1.25rem;
      margin: 0 auto 3.5rem;
    }

    .hero__cta {
      gap: 1.5rem;
    }
  }

  @media (min-width: 1024px) {
    .hero {
      height: unset;
    }

    .hero::after {
      content: none;
      display: none;
    }

    .hero__wave {
      bottom: 0;
    }

    .hero__inner {
      grid-template-columns: 1.1fr 1fr;
      text-align: unset;
    }

    .hero__title {
      font-size: clamp(2.6rem, 6.5vw, 5rem);
    }

    .hero__lead {
      margin: 0 0 3rem;
      padding-left: 0;
      text-align: left;
    }

    .hero__cta {
      justify-content: flex-start;
    }

    .hero__pills {
      justify-content: flex-start;
    }

    .hero__media {
      display: grid;
    }

    .hero__scroll {
      display: grid;
    }
  }

  /* Narrow desktop: two-column hero — heading reads huge vs logo; keep title smaller than full desktop clamp */
  @media (min-width: 1024px) and (max-width: 1155px) {
    .hero__title {
      font-size: 3.5rem;
      margin: 0rem 0 3rem;
    }
  }

  /* Wide desktop (1156px+) — optional overrides; stacks after min-width 1024 rules */
  @media (min-width: 1156px) {
    .hero__title {
      font-size: 4.4rem;
    }
    /* Add styles here */
  }

  .hero__scroll:hover {
    border-color: var(--color-blush-deep);
  }

  .hero__scroll span {
    width: 4px;
    height: 8px;
    background: var(--color-blush-deep);
    border-radius: 2px;
    animation: scroll-down 1.6s ease-in-out infinite;
  }

  @keyframes scroll-down {
    0% {
      transform: translateY(0);
      opacity: 1;
    }
    80% {
      transform: translateY(14px);
      opacity: 0;
    }
    100% {
      transform: translateY(0);
      opacity: 0;
    }
  }
</style>
